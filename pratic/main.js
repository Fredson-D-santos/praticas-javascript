alert("Olá"); // Exibe uma mensagem de alerta

var nome = prompt("Qual é o seu nome?"); // Pede ao usuário para inserir o nome e armazena na variável nome

if (nome !== null) { // Verifica se o usuário não clicou em "Cancelar"
    alert(`Olá, ${nome}!`); // Exibe uma mensagem de alerta com o nome digitado pelo usuário
}

